plugins {
    id "org.ajoberstar.github-pages" version "1.5.1"
}

githubPages {
    repoUri = "https://github.com/arekbee/gradle-R-plugin.git"
    pages {
        from(file('build/docs/groovydoc')) {
            into '.'
        }
    }
    deleteExistingFiles = true
}